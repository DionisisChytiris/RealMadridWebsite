/* eslint-disable no-unused-vars */
import React, {useEffect} from 'react'

const Schedule = () => {
    useEffect(() => {
        window.scrollTo(0, 0);
      }, []);
  return (
    <div>Schedule</div>
  )
}

export default Schedule